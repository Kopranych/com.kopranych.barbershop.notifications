spring:
  application:
    name: notifications
  cloud:
    config:
      uri: http://localhost:8888
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration